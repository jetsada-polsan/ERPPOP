# Copy to deploy/env.local when running manually.
# Do not commit deploy/env.local.

GIT_REMOTE_URL=
GIT_BRANCH=main

SSH_USER=root
SSH_HOST=27.254.143.219
SSH_PORT=22
REMOTE_PATH=/var/www/jeterp

# Leave empty for normal source-only deploys.
# Set to 1 only when composer.json/package assets changed and the server can run it.
RUN_COMPOSER_INSTALL=0
RUN_NPM_BUILD=0
