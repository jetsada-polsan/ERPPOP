<!doctype html>
<html lang="th"><head><meta charset="utf-8"><title>ป้ายชั่ง {{ $document->doc_number }}</title>
<style>
*{box-sizing:border-box}body{margin:0;padding:8mm;background:#e5e7eb;font-family:Tahoma,sans-serif}.toolbar{margin-bottom:6mm}.toolbar button{border:0;background:#111827;color:#fff;padding:9px 16px;font-weight:700;cursor:pointer}.sheet{display:grid;grid-template-columns:repeat(3,60mm);gap:4mm}.label{width:60mm;height:40mm;background:#fff;border:1px dashed #64748b;padding:2.2mm;display:grid;grid-template-rows:auto 1fr auto;overflow:hidden}.brand{display:flex;justify-content:space-between;align-items:start}.brand strong{font-size:9pt}.brand small{font-size:6.5pt}.name{font-size:10pt;font-weight:800;line-height:1.1}.numbers{display:flex;justify-content:space-between;align-items:end}.weight{font-size:12pt;font-weight:800}.price{font-size:15pt;font-weight:900}.barcode svg{width:100%;height:15mm;display:block}@media print{body{padding:0;background:#fff}.toolbar{display:none}.sheet{grid-template-columns:repeat(3,60mm)}.label{border:0;page-break-inside:avoid}}
</style></head><body onload="window.print()">
<div class="toolbar"><button onclick="window.print()">พิมพ์ {{ $batch->packages->count() }} ป้าย</button></div>
<div class="sheet">@foreach($batch->packages as $package)<div class="label"><div class="brand"><div><strong>POP STAR 4M</strong><div class="name">{{ $batch->outputProduct->name_th }}</div></div><small>{{ $document->doc_number }}-{{ $package->seq }}</small></div><div class="barcode">{!! $barcodes->svg($package->barcode) !!}</div><div class="numbers"><div><div class="weight">{{ number_format($package->weight_qty,3) }} กก.</div><small>฿{{ number_format($package->unit_price,2) }}/กก.</small></div><div class="price">฿{{ number_format($package->total_price,2) }}</div></div></div>@endforeach</div>
</body></html>
